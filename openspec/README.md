# 給 IT 工程師的快速導讀

## 這是什麼

盤點班表調移系統的前端交接版。**純靜態 SPA，無資料庫、無寫入需求。**

排班人員匯入 Excel 班表 → 拖曳調整人員與門市 → 匯出 Excel。系統會比對人員與休假資料，提示「某人今天被排太多間」或「已休假卻被排班」。

---

## 你需要做的事

**只有兩支唯讀 GET API。** 其餘功能前端已完成，不需後端參與。

| API | 用途 |
|---|---|
| `GET /api/staff/roster?dept=北一課` | 人員通訊錄（含關鍵欄位「班表代號」） |
| `GET /api/staff/leave?dept=北一課&yearMonth=202608` | 人員休假日期 |

完整欄位規格與 Request／Response 範例見 **`api-interface.json`**。

### 程式進入點

所有待串接處集中在 **`src/services/staffApi.js`**，以 `// TODO: IT 工程師請在此串接後端 API` 標註。呼叫時機在 `src/App.jsx` 的 `useEffect`（班表匯入後自動觸發），一般情況不需修改。

### API 位址設定

於專案根目錄建立 `.env`：

```
VITE_API_BASE_URL=https://your-api-host/api
```

未設定時預設走同網域 `/api`。

---

## 本機執行

```bash
npm install
npm run dev
```

```bash
npm run build
```

建置產物在 `dist/`，可直接放上任何靜態主機。

**API 尚未完成也能跑**：連線失敗時畫面會顯示「人員資料 API 尚未連線」，出勤統計退回只計算班表內出現的代號，班表匯入／拖曳／匯出完全不受影響。你可以先跑起來看實際畫面再決定 API 怎麼實作。

---

## 三個容易踩到的重點

1. **「班表代號」是整套統計的基礎**
   班表的「預定盤點者」欄位存的是單字代號串（例如「于筱董」代表三個人），前端會逐字拆解後比對通訊錄的「班表代號」欄位。此欄位在同一課別內必須唯一。

2. **休假日期請回傳完整 yyyyMMdd**
   原 Excel 是「8/19、8/20、8/21」且沒有年份。請由後端展開成 `["20260819","20260820","20260821"]`，前端不再做字串拆解。

3. **兩支 API 的「姓名」必須完全一致**
   前端以「姓名」關聯休假紀錄與人員資料。若有全形半形或空白差異會對不上。

---

## 想快速理解商業邏輯

看 `SDD.md` 的第 3.3 節（當日出勤列的計算規則）與第 6 節（實測發現的資料品質問題）。這兩節說明了為什麼要「只統計店號有填的列」以及「為什麼 3 間店不算錯誤」。

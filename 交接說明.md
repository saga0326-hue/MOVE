# 盤點班表調移系統 — 交接說明

本包含前端完整原始碼、技術文件與功能說明書。

---

## 這是什麼

排班人員匯入 Excel 班表，以拖曳方式調整門市與人員配置，再匯出回 Excel。
系統會自動偵測「排班次數過多」「休假卻被排班」「同一間店排兩次」等問題。

**純靜態 SPA · 無資料庫 · 不寫入任何資料**

---

## 檔案導覽

| 路徑 | 內容 | 給誰看 |
|---|---|---|
| `docs/盤點班表調移系統_功能說明書.docx` | 完整功能說明（Word） | **一般使用者、主管** |
| `openspec/README.md` | IT 快速導讀（5 分鐘） | **IT 工程師先看這份** |
| `openspec/api-interface.json` | API 介接規格 | IT 工程師 |
| `openspec/SDD.md` | 系統設計文件（完整規格、計算規則） | IT 工程師 |
| `src/` | 前端原始碼 | IT 工程師 |

---

## IT 需要做的事

**只有兩支唯讀 GET API**，其餘功能前端已完成。

| API | 用途 |
|---|---|
| `GET /api/staff/roster?dept=北一課` | 人員通訊錄（姓名、職稱、班表代號） |
| `GET /api/staff/leave?dept=北一課&yearMonth=202608` | 人員休假日期與假別 |

**唯一需要修改的檔案**：`src/services/staffApi.js`（待串接處皆有 `// TODO` 標註）

API 位址於 `.env` 設定 `VITE_API_BASE_URL`，未設定時預設 `/api`。

---

## 如何執行

### 方式一：不需安裝，點兩下就能用

`盤點班表調移系統.html` 是把整個系統打包成的**單一檔案**（JS、CSS 全部內嵌），
直接用瀏覽器開啟即可使用，不需要 Node.js、不需要伺服器，也可以直接用郵件寄送。

> 一般的網頁建置產物無法用 file:// 直接開啟——Chrome 會以 CORS 政策擋下外部
> ES module，畫面會空白。因此本檔案將所有資源內嵌以避開該限制。

重新產生此檔案：

```bash
npm run build:standalone
```

產物在 `dist-standalone/index.html`。

### 方式二：開發模式

```bash
npm install
npm run dev
```

### 方式三：部署到伺服器

```bash
npm run build
```

產物在 `dist/`，可放上任何靜態主機。

**API 尚未完成也能跑** — 會顯示「人員資料 API 尚未連線」提示，但班表匯入、拖曳、編輯、匯出、店暫存區、門市重複警告全部正常運作。建議先跑起來看畫面，再決定 API 怎麼實作。

---

## 三個容易誤解的重點

### 1. 工號對照不需要 API 提供

班表的「盤點1～8」欄位依序就是「預定盤點者」每個代號的工號。系統匯入時會自動建立對照表（實測 390 列建出 28 個代號、零衝突），所以通訊錄 API 的價值在於**姓名、職稱、完整名單**，而不是工號。

### 2.「人力」與「盤點1～8」屬於人員資料

拖曳互換人員時，這些欄位必須跟著人一起移動，不能留在原地。
（此處原本有 bug，v1.1.0 已修正並以 390 列資料驗證）

### 3. 休假日期請回傳完整 yyyyMMdd

原 Excel 是「8/19、8/20、8/21」且無年份，請由後端展開成 `["20260819","20260820","20260821"]`。

---

## 版本

v1.1.0
